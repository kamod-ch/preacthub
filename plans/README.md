# Implementation Plans

Generated by the improve skill on 2026-07-07 at commit `c356011`. Execute in the order below unless dependencies say otherwise. Each executor: read the assigned plan fully before starting, honor its STOP conditions, and update your row when done.

## Verification baseline

At audit time these commands were verified:

- `npm run typecheck` — passed
- `npm run test` — passed, 11 tests
- `npm run check` — passed, 126 routes, `No issues found.`
- `npm run lint` — completed with 2 unused-import warnings
- `npm audit --audit-level=high` — no high/critical advisories; one moderate `js-yaml` advisory exists

`npm run build` was not run during audit because it writes generated output under `dist/`.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Validate library, category, and route slug collisions | P1 | S | — | DONE |
| 002 | Add tests for library filters and URL state | P1 | M | — | DONE |
| 003 | Standardize the repo on npm commands and lockfile | P2 | S | — | DONE |
| 004 | Add automated content quality validation for library entries | P2 | M | 001 | DONE |
| 005 | Cache library directory data during page metadata generation | P2 | M | 001 | TODO |
| 006 | Clean lint warnings and make lint failures actionable | P3 | S | — | TODO |
| 007 | Review and resolve the transitive js-yaml advisory | P3 | S | 003 | TODO |
| 008 | Design and implement first-class library compare pages | P2 | L | 001, 005 | DONE |
| 009 | Add a reproducible library verification workflow | P2 | M | 004 | TODO |
| 010 | Add a helper for turning library submission issues into draft entries | P3 | M | 003, 004 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 004 depends on 001 because content quality validation should build on the completed slug/route invariants.
- 005 depends on 001 because caching route data before collision validation can hide route-generation errors during repeated checks.
- 007 depends on 003 so only the canonical lockfile is updated when resolving the advisory.
- 008 depends on 001 and 005 because compare pages add more routes and should use validated, cached directory metadata.
- 009 depends on 004 because verification workflow docs/tests should fit the finalized content quality rules.
- 010 depends on 003 and 004 because generated drafts should use canonical commands and satisfy content quality validation.

## Findings considered and rejected

- No high-confidence findings were rejected in this audit. Lower-confidence ideas such as adding live npm download/GitHub star metrics were not planned because they require external data sourcing and would complicate the static-site architecture.

## Notes for executors

- Do not commit generated `dist/` files unless the maintainer explicitly asks.
- Prefer npm commands unless Plan 003 is rejected or rewritten.
- Keep compatibility and verification claims conservative; do not invent `lastVerified` dates.
